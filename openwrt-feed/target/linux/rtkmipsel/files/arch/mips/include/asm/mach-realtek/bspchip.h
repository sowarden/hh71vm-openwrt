/*
 * Compatibility: some vendor drivers Realtek connect a register map
 * like <asm/mach-realtek/bspchip.h>. Let's keep the redirect here, not the second one
 * a copy of the file so that the register definitions live in the tree in one copy.
 */
#ifndef __ASM_MACH_REALTEK_BSPCHIP_H
#define __ASM_MACH_REALTEK_BSPCHIP_H

#include <asm/mach-rtl8197f/bspchip.h>

#endif /* __ASM_MACH_REALTEK_BSPCHIP_H */
