{
  "schema": 1,
  "changes": [
    "Restore Wi-Fi startup by preserving executable permissions on the rtl8192cd control utility.",
    "Fix the Backup / Flash Firmware page by loading the signed firmware updater as a valid LuCI class.",
    "Restore fast network-free firmware identity status with the OpenWrt jshn library under nounset.",
    "Bound signed update checks to the LuCI request window while keeping release history optional."
  ]
}
