{
  "schema": 1,
  "changes": [
    "Return the verified latest signed firmware release promptly and load optional history separately.",
    "Avoid parallel legacy IPK output races during clean builds.",
    "Publish kmod-pppol2tp in the matching signed package feed.",
    "Preserve or remove modem-reported current-only LTE bands safely without fixed operator band lists."
  ]
}
