{
  "schema": 1,
  "changes": [
    "Fix Xray filling the router's RAM over time: an upstream connection leak is patched and its memory use is now bounded, with a watchdog guard as a backstop."
  ]
}
